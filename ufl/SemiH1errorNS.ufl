# Copyright (C) 2015 PSU CCMA
#
# Compute the Semi H1 error of a computed solution.
#
# Compile this form with FFC: ffc -O -l dolfin SemiH1Error.ufl.

CG = FiniteElement("CG", tetrahedron, 1)
Vit = FiniteElement("RT", tetrahedron, 1)

error = Coefficient(CG)
errorU = Coefficient(Vit)

M =  inner(grad(errorU[0]),grad(errorU[0]))*dx + inner(grad(errorU[1]),grad(errorU[1]))*dx + inner(grad(errorU[2]),grad(errorU[2]))*dx \
	+ inner( grad(error), grad(error) ) * dx

